apiVersion: 1

providers:
  - name: webengine-dashboards
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/dashboards
