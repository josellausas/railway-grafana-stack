ARG VERSION=latest

FROM grafana/tempo:${VERSION}

# Tempo image is distroless (no /bin/sh); cannot run entrypoint script.
# Run as root so Railway volume at /var/tempo is writable.
USER root

COPY tempo.yml /etc/tempo/tempo.yml

CMD ["-config.file=/etc/tempo/tempo.yml"]